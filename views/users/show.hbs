<div>
    <div>
        <a href="/users">Back to All Users</a>
    </div>

    <h1>{{user.firstName}} {{user.lastName}}</h1>
    <div>
        <img src="{{user.photoUrl}}" alt="{{user.username}}'s prophile photo">
    </div>
    <div>
        <div>Username: {{user.username}}</div>
        <div>Email: {{user.email}}</div>
    </div>
</div>